// Function declaration
const myFunc = function(num1, num2) {
  // Sum the two numbers and log the result
  console.log(num1 + num2);
}

// Test the function by calling it with different sets of arguments
myFunc(5, 1);  // Output: 6
myFunc(4, 20);   // Output: 24
myFunc(4, 5); // Output: 9