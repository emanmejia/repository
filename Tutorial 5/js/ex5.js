// Loop to generate 10 random integers
for (let i = 0; i < 10; i++) {
  // Generate a random integer between 1 and 10 inclusive
  let randomInt = Math.floor(Math.random() * 10) + 1;
  console.log(randomInt);
} 